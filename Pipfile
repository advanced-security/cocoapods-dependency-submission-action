[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests = "*"

[dev-packages]
black = "*"

[scripts]
main = "python -m myaction"
test = "python -m unittest discover -v -s ./tests -p 'test_*.py'"

# formatting / linting
fmt = "black ."
lint = "black --check ."

